# Copy this file to .env, then paste the key your instructor shares in class.
# .env is gitignored, never commit your real keys.

# Blitz API: used by scripts/source_leads.py to pull a real target list (TAM)
# with LinkedIn URLs and (optionally) verified emails.
BLITZAPI_API_KEY=

# Optional, only if you want to verify emails yourself:
# KITT_API_KEY=
